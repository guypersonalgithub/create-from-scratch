import { StyledInteractiveTitle } from "../../styledComponents/StyledInteractiveTitle";

export const JavascriptRoot = () => {
  return (
    <div>
      <StyledInteractiveTitle>Javascript</StyledInteractiveTitle>
    </div>
  );
};
