import { StyledMainTitle } from "../../styledComponents/StyledMainTitle";

export const HardwareRoot = () => {
  return (
    <div>
      <StyledMainTitle>Hardware</StyledMainTitle>
    </div>
  );
};
