import { StyledMainTitle } from "../../styledComponents/StyledMainTitle";

export const Cgroups = () => {
  return (
    <div>
      <StyledMainTitle>Cgroups</StyledMainTitle>
      <div>Relevant for security and resources management.</div>
      <div>Cgroups help us manage resources of containers, how much resources are allocated to each container.</div>
    </div>
  );
};
