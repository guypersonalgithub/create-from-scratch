import { StyledLink } from "../../styledComponents/StyledLink";
import { StyledMainTitle } from "../../styledComponents/StyledMainTitle";

export const BinaryRoot = () => {
  return (
    <div>
      <StyledMainTitle>Binary</StyledMainTitle>
      <StyledLink pathname="/binary/base-10">Base 10 to binary</StyledLink>
    </div>
  );
};
