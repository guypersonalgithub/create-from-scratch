{
  "name": "docker",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "detect": "cd scripts && node detectPackages",
    "postinstall": "cd scripts/docker && ts-node generateDockerComposeDev"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "js-yaml": "^4.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
